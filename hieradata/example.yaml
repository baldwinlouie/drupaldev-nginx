---
sites:
    vhosts:
        example.drupal.dev:
            server_name: leadtail.drupal.dev
            server_aliases:
                - www.leadtail.drupal.dev
            www_root: /var/www/leadtail.drupal.dev
            listen_port: '80'
            index_files:
                - index.php
    databases:
        example:
            grant:
                - ALL
            host: localhost
            name: example
            user: example
            password: example
            sql_file: ''
    solr:
        - example
